// backend/prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(cuid())
  googleId  String   @unique
  email     String   @unique
  name      String?
  avatar    String?
  customAvatar String?  // Kullanıcının yüklediği fotoğraf
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  swipes    UserWordSwipe[]
  gameResults UserGameResult[]
}

model Word {
  id        String   @id @default(cuid())
  text      String   @unique
  meaning   String
  example   String?
  level     String?   // A1, A2, B1, vs.
  createdAt DateTime @default(now())

  swipes    UserWordSwipe[]
}

model UserWordSwipe {
  id        String       @id @default(cuid())
  userId    String
  wordId    String
  status    SwipeStatus  // viewed | learned | skipped
  createdAt DateTime     @default(now())

  user  User @relation(fields: [userId], references: [id])
  word  Word @relation(fields: [wordId], references: [id])

  @@index([userId, wordId])
}

enum SwipeStatus {
  VIEWED
  LEARNED
  SKIPPED
}

model UserGameResult {
  id        String   @id @default(uuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  
  gameType  String   // 'QUIZ', 'SCRAMBLE', 'MEMORY', 'DICTATION'
  score     Int      // Kazanılan puan
  correct   Int      // Doğru sayısı
  wrong     Int      // Yanlış sayısı
  
  createdAt DateTime @default(now())
}